<section id="eventadmin_threads">
	<title>Asynchronous processing and the event bus</title>
	<indexterm>
		<primary>Event service</primary>
		<secondary>Asynchronous processing</secondary>
	</indexterm>

	<para>
		Your threads can use the
		<code>IEventBroker</code>
		to send event data. Every listener will be automatically called and if
		a method is annotated with the
		<code>UIEventTopic</code>
		annotation, it will be
		called in the main thread.
	</para>

	<para>
		<programlisting language="java">
			<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
				parse="text" href="./examples/event/Example.java" />
		</programlisting>
	</para>


</section>
