<section id="emfmodel">
	<title>Generating the domain classes</title>
	<section id="emfmodel_code">
		<title>Generating Java code</title>
		<para>
			Based on the
			<filename>.genmodel</filename>
			files, you can generate
			Java
			code.
		</para>
		<para>
			Right-click
			on the root node of the
			<filename>.genmodel</filename>
			file
			and select
			<guilabel>Generate Model Code</guilabel>
			. This
			creates the
			Java
			implementation of the
			EMF model in
			the
			current
			project.
		</para>
		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/EMFCodeGen10.gif" />
				</imageobject>
			</mediaobject>
		</para>
	</section>

	<section id="emfmodel_codereview">
		<title>Review the generated code</title>
		<para>
			The generated code will consist of the following:
			<itemizedlist>
				<listitem>
					<para><filename>model</filename> -- Interfaces and the Factory to create
						the Java classes
					</para>
				</listitem>
				<listitem>
					<para><filename>model.impl</filename> -- Concrete implementation of the
						interfaces
						defined in model
					</para>
				</listitem>
				<listitem>
					<para><filename>model.util</filename> -- The AdapterFactory</para>
				</listitem>
			</itemizedlist>
		</para>
		<para>
			The central factory has methods for creating all defined
			objects
			via
			<code>createObjectName()</code>
			methods.
		</para>
		<para>
			For each attribute the generated interface and its
			implementation
			contain
			<code>getter</code>
			and (if allowed in the model
			definition)
			<code>setter</code>
			methods. Each setter also has a generated
			notification to observers of
			the model. This means that other objects
			can attach them to
			the model
			and
			react to changes in the model.
		</para>

		<para>
			Each generated interface extends the
			<code>EObject</code>
			interface.
			<code>EObject</code>
			is the
			base of every EMF class and is the EMF
			equivalent of
			<code>java.lang.Object</code>
			.
			<code>EObject</code>
			and its corresponding
			implementation class
			<code>EObjectImpl</code>
			provide a lightweight base
			class that
			lets the generated
			interfaces and
			classes
			participate in the EMF
			notification and
			persistence
			frameworks.
		</para>

		<para>
			Every generated method is tagged with
			<code>@generated</code>
			. If you
			want to
			manually adjust the method
			and have
			EMF overwrite the
			method during the next generation run,
			you
			need to
			remove this tag.
		</para>

	</section>
	<section id="emfmodel_update">
		<title>Updating the model</title>
		<para>
			If you change your
			<filename>.ecore</filename>
			model then you can
			update the
			<filename>.genmodel</filename>
			simply by
			reloading.
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/EMFModel70.gif" />
				</imageobject>
			</mediaobject>
		</para>
	</section>
</section>
