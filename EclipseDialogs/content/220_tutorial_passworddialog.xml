<section id="tutorial_passworddialog">
	<title>Creating a password dialog</title>
	<para>
		Create a
		<code>com.example.e4.rcp.todo.dialogs</code>
		package in your application plug-in.
	</para>
	<para>
		Create a new class called
		<wordasword>PasswordDialog</wordasword>
		which extends the
		<wordasword>Dialog</wordasword>
		class. This dialog allows you to enter a user name and a
		password.
	</para>
	<para>
		<mediaobject>
			<imageobject>
				<imagedata fileref="images/credentials10.png" scale="60" />
			</imageobject>
			<textobject>
				<phrase>Enter user credentials</phrase>
			</textobject>
		</mediaobject>
	</para>
	<para>
		<programlisting language="java">
			<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
				parse="text" href="../examples/preferences/PasswordDialog.java" />
		</programlisting>
	</para>

	<para>
		To use the dialog in your application,
		create a new
		menu entry
		(and a
		command with a handler) which
		allows you
		to
		open this
		dialog. The
		class
		associated with the handler should be
		called
		<wordasword>EnterCredentialsHandler</wordasword>
		.
	</para>

	<para>
		<programlisting language="java">
			<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
				parse="text" href="../examples/preferences/EnterCredentialsHandler.java" />
		</programlisting>
	</para>

	<warning>
		<para>
			Ensure that your handler links to the
			<code>EnterCredentialsHandler</code>
			class and not the
			<code>PasswordDialog</code>.
		</para>
	</warning>

</section>