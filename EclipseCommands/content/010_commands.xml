<section id="commands">
	<title>Commands</title>
	<section id="commands_overview">
		<title>What are commands?</title>
		<para>
			A command in Eclipse is a declarative description of a
			component
			and
			is
			independent from the implementation details.
		</para>

		<para>
			A
			command can be
			categorized, assigned
			to
			the user
			interface and a
			key
			binding can be defined for the
			command.
			The behavior
			of a command is
			defined via a handler.
		</para>
	</section>

	<section id="commands_process">
		<title>Relevant Extension Points for Commands</title>
		<para>
			To define a command you create an extension to the
			<code>org.eclipse.ui.command</code>
			extension point.
		</para>
		
	</section>




</section>