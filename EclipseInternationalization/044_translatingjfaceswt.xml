<section id="i18n_sourceswt">
	<title>Translating SWT and JFace code</title>
	<para>The SWT and JFace plug-ins include their resource translation
		files. You can change these default texts and their
		translations
		because resource
		bundles have an override support built
		in.
	</para>
	<para>
		If you
		supply
		a different file
		of the SWT or JFace translation
		bundles in
		a
		fragment or
		plug-in
		(such as
		SWTMessage_de.properties),
		then
		the most
		local bundle
		matching
		the
		current locale is used.
	</para>
	<para>
		To change the text supplied by SWT and JFace do the following:
		<itemizedlist>
			<listitem>
				<para>
					Create two fragments projects, which will hold the translations. One with org.eclipse.jface and one with org.eclipse.swt as
					Host-Plug-in.
				</para>
			</listitem>
			<listitem>
				<para>
					Create the
					<code>org.eclipse.jface</code>
					and
					<code>org.eclipse.swt.internal</code>
					packages in the corresponding fragments.
				</para>
			</listitem>
			<listitem>
				<para>
					Copy the
					<filename>org/eclipse/jface/message.properties</filename>
					file from the org.eclipse.jface plug-in and the
					<filename>org/eclipse/swt/internal/SWTMessage.properties
					</filename>
					file
					from the SWT plug-in to
					the
					corresponding packages in your
					project. See the appendix for the links to their
					location in the
					Eclipse Git repository.
				</para>
			</listitem>
			<listitem>
				<para>Provide the files with your desired language extensions, e.g.
					message_de.properties.
				</para>
			</listitem>
			<listitem>
				<para>Edit the files to contain your text properties.</para>
			</listitem>
		</itemizedlist>
	</para>
	<warning>
		<para>
			The original
			<filename>message.properties</filename>
			in JFace or
			<filename>SWTMessage.properties</filename>
			in SWT cannot be overridden by a
			fragment.
			Therefore, you can only
			provide additional
			<filename>*.properties</filename>
			files
			for a
			certain locale like
			message_de.properties.
		</para>
	</warning>
	<para>
		You can translate selected
		properties of the
		original
		<filename>message.properties</filename>
		file.
		The
		following listing shows how you
		would override the buttons of a
		JFace
		<code>Wizard</code>
		class in a
		<filename>message_de.properties</filename>
		file.
	</para>
	<para>
		<programlisting language="java">
			<xi:include xmlns:xi="http://www.w3.org/2001/XInclude" parse="text" href="./examples/jface/message.example" />
		</programlisting>
	</para>

	<mediaobject>
		<imageobject>
			<imagedata fileref="images/jface_translation_fragment_packagestructure.png" scale="60" />
		</imageobject>
		<textobject>
			<phrase>Package structure of the JFace fragment</phrase>
		</textobject>
	</mediaobject>
</section>
