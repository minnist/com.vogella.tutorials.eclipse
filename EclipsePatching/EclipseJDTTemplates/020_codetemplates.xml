<section id="jdt_codetemplates">
	<title>Using extension points to define JDT templates</title>
	<para>You can also define your templates within an Eclipse plug-in.
		This makes it easier to distribute your templates to other developers.
	</para>
	<para>
		Providing a template can be done via an extension to the
		<code>org.eclipse.ui.editors.templates</code>
		extension point.
	</para>
	<para>
		The templates are activated for a certain scope. For example you have
		templates for statements within a method or templates for new methods.

		<table frame='all'>
			<title>context in the template</title>
			<tgroup cols='2' align='left' colsep='1' rowsep='1'>
				<colspec colname='c1' colwidth="1*" />
				<colspec colname='c2' colwidth="2*" />
				<thead>
					<row>
						<entry>Statement</entry>
						<entry>Description</entry>
					</row>
				</thead>
				<tbody>
					<row>
						<entry>java-statements</entry>
						<entry>Templates which are available within a method.</entry>
					</row>
					<row>
						<entry>java-members</entry>
						<entry>Allows to define templates for members, e.g., new methods.
						</entry>
					</row>
				</tbody>
			</tgroup>
		</table>
	</para>

	<para>
		To provide a code templates based on this extension point, create a
		new plug-in called
		<code>com.vogella.jdt.templates</code>
		.
	</para>
	<para>
		Add an extension for the
		<code>org.eclipse.ui.editors.templates</code>
		extension point and add an
		<code>include</code>
		as in the following screenshot.
	</para>
	<para>
		<mediaobject>
			<imageobject>
				<imagedata fileref="images/jdttemplates10.png"/>
			</imageobject>
			<textobject>
				<phrase></phrase>
			</textobject>
		</mediaobject>
	</para>

	<para>In the reference file you can add your templates following a
		predefined schema. The file can look like the following, the structure
		should be easily to adjust for other use cases.
	</para>
	<para>
		<programlisting language="java">
			<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
				parse="text" href="./examples/additional-templates.xml" />
		</programlisting>
	</para>



</section>