<section id="theming">
	<title>Theming</title>
	<section id="theming_overview">
		<title>Overview</title>
		<para>A Eclipse RAP application look similar to a Eclipse RCP
			application running in a browser. While this is cool, it is not
			necessary always what you want if you develop a web application.
		</para>
		<para> Eclipse RAP therefore provides the possibility to change the
			look of the application via theming. Themes can be defined in css
			like
			files. The following will give an demonstration how to do this.
		</para>
	</section>
	<section id="theming_example">
		<title>Example</title>
		<para>Create a new Eclipse RAP project "de.vogella.rap.theming". Use
			the "RAP Mail Template" as template.  </para>
		<para> Create a new folder "themes" in your project. </para>

		<para> Add the directory themes to your build.properties. Otherwise
			the files from themes will not get included in an exported product.
		</para>
		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/theming08.gif" />
					
				</imageobject>
			</mediaobject>
		</para>
		<para> Add the extension point "org.eclipse.rap.ui.themes". Use the
			following settings. </para>

		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/theming10.gif" />
					
				</imageobject>
			</mediaobject>
		</para>

		<para> Create now the following file "theme.css" in the folder themes.
		</para>

		<para>
			<programlisting>
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="../examples/themes/theme.css" />
			</programlisting>
		</para>

		<para> Add now the "org.eclipse.rap.ui.branding" extension point which
			the following settings. Important is the servletName "rap" as this is
			used in your runtime configuration, your entrypoint and the theme you
			want to use.  
	</para>
		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/theming20.gif" />
					
				</imageobject>
			</mediaobject>
		</para>

		<para> This will result in the following rap application display.
		</para>

		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/theming30.gif" />
					
				</imageobject>
			</mediaobject>
		</para>


		<para> For additional properties in the style sheet check the RAP
			Theming Wiki (link below). </para>
	</section>

</section>